<p id="notice"><%= notice %></p>

  <p>
    <strong>nickname:</strong>
    <%= @user.nickname %>
  </p>

  <%= @microposts.each do |form| %>

    <p>
      <strong>text</strong>
      <%= form.text %>
    </p>

    <p>
      <strong>tag:</strong>
      <%= form.tag %>
    </p>

    <%= link_to 'Edit', edit_micropost_path(@microposts) %> 

    <% end %>

<%= link_to 'Back', microposts_path %>
