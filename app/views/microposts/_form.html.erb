<div class="container">
  <div class="table-responsive">
    <tr>
      <h3><span class="badge badge-success"><th scope="col">Nickname: <%= @user.nickname %></th></span></h3>
      <th colspan="3"></th>
    </tr>
      <%= form_for(@micropost) do |form| %>
              <div class="field">
                  <%= form.text_area :text, class: 'form-control', placeholder: "Text", autofocus: true  %><br>
              </div>
              <div class="actions">
                <%= form.submit :class => "btn btn-primary"%>
              </div>
            <% end %><br>
        <%= link_to 'Back', microposts_path ,:class => "btn btn-secondary"%>
  </div>
</div>
