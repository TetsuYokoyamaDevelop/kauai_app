<p id="notice"><%= notice %></p>
<h1 class ="text-center">My Post History</h1>
<div class="container">
  <div class="button_wrapper">
    <%= link_to 'New MicroPost', new_micropost_path ,:class => "btn btn-primary" %>
    </div>
    <div class="alt-table-responsive">
          <table class="table">
            <thead class="thead-dark">
              <tr>
                <th scope="col">Nickname</th>
                <th scope="col">Text</th>
                <th scope="col">Tag</th>
                <th colspan="3"></th>
              </tr>
            </thead>
        <tbody>
          <% @microposts.each do |micropost| %>
            <tr>
                <div class="row">
                    <div class="col-sm">
                        <td class="col-xs-3 col-ms-3 col-md-4 col-lg-4"><%= micropost.user.nickname %></td>
                    </div>

                    <div class="col-sm">
                        <td class="col-xs-3 col-ms-3 col-md-4 col-lg-4"><%= micropost.text %></td>
                    </div>

                    <div class="col-sm">
                      <td class="col-xs-3 col-ms-3 col-md-4 col-lg-4"><%= micropost.tag %></td>
                    </div>

                    <div class="col-sm">
                      <td><%= link_to 'Comment', micropost_comments_path(micropost) ,:class => "btn btn-info"%></td>
                    </div>

                </div>
            </tr>
            <tr>
              <td><%= link_to 'show', micropost_path(micropost) %></td>
              <td><%= link_to 'Destroy', micropost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>
</div>
